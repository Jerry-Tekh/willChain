# GenLayer contract testing + Python deploy script.
# The Python deploy path (scripts/deploy_py.py --chain testnet_bradbury) is the
# smallest-footprint way to deploy to Testnet Bradbury: genlayer-py pulls in
# web3 only, versus the CLI which bundles a full JS SDK + Docker tooling.
genlayer-test
genlayer-py>=0.18.0   # first releases exporting the testnet_bradbury chain
pytest
python-dotenv>=1.0.0
